(jbuild_version 1)

; Oh Jbuilder ...
;(executables (
;  (names (bench compare))
;  (public_names (bench compare))
;  (libraries (profiling_lib core_bench))
;  (preprocess (pps (bisect_ppx -conditional)))
;  (ocamlopt_flags -O3)))

(executable (
  (name bench )
  (public_name bench )
  (modules (bench))
  (libraries (profiling_lib core_bench))
  (preprocess (pps (bisect_ppx -conditional)))
  (ocamlopt_flags -O3)))

(executable (
  (name compare )
  (public_name compare )
  (modules (compare))
  (libraries (profiling_lib core_bench))
  (preprocess (pps (bisect_ppx -conditional)))
  (ocamlopt_flags -O3)))

